services:
  frontend:
    build: ./api-test
    container_name: frontend
    restart: always
    ports:
      - "80:8080"
    networks:
      - appnet
# 网络配置
networks:
  appnet:
#    driver: bridge
    external: true
